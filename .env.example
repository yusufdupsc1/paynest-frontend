# PayNest Frontend — Environment Variable Reference
# Copy to .env.local and set values for your environment

# Backend API origin (no trailing slash)
# Local development example: http://localhost:3000
# Vercel production required format: https://<service>.onrender.com
NEXT_PUBLIC_API_URL=https://your-render-service.onrender.com

# Do not use localhost for production builds.
